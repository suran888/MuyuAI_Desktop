npm start
   ↓
build.js (编译 TSX/React → JS)
   ↓
electron 启动 → src/index.js (主进程入口)
   ↓
┌─────────────────────────────────┐
│ 初始化阶段                      │
├─────────────────────────────────┤
│ 1. 加载环境变量 (.env)          │
│ 2. 单实例锁定                   │
└─────────────────────────────────┘
   ↓
app.whenReady()
   ↓
┌─────────────────────────────────┐
│ 核心服务初始化                  │
├─────────────────────────────────┤
│ 1. databaseInitializer          │
│ 2. authService                  │
│ 3. modelStateService            │
│ 4. featureBridge (IPC 注册)     │
│ 5. windowBridge                 │
│ 6. ollamaService (后台预热)     │
└─────────────────────────────────┘
   ↓
createWindows() → windowManager.js
   ↓
┌─────────────────────────────────┐
│ 窗口创建                        │
├─────────────────────────────────┤
│ Header 窗口 (353x47)            │
│  ├─ 加载 header.html            │
│  ├─ 预加载 preload.js           │
│  └─ 执行 headerApp.tsx          │
└─────────────────────────────────┘
   ↓
HeaderController 初始化
   ↓
┌─────────────────────────────────┐
│ 状态判断                        │
├─────────────────────────────────┤
│ Passcode? → WelcomeHeader       │
│ Permission? → PermissionPanel   │
│ API Key? → ApiKeyHeader         │
│ All OK? → Main State            │
└─────────────────────────────────┘
   ↓
如果 state = 'main'
   ↓
┌─────────────────────────────────┐
│ 创建功能窗口                    │
├─────────────────────────────────┤
│ - main (主界面)                 │
│ - listen (实时转录)             │
│ - ask (AI 问答)                 │
│ - screenshot (截图分析)         │
│ - transcript (完整转录)         │
│ - settings (设置)               │
│ - shortcut-settings (快捷键)    │
└─────────────────────────────────┘
   ↓
程序完全启动 ✅

📝 关键文件总览
文件路径	功能	类型
package.json	定义启动脚本	配置
build.js	编译 TSX/React → JS	构建工具
src/index.js	Electron 主进程入口	主进程
src/preload.js	安全 API 注入	预加载
src/window/windowManager.js	窗口创建与管理	主进程
src/window/windowLayoutManager.js	窗口布局计算	主进程
src/window/smoothMovementManager.js	窗口动画	主进程
src/bridge/featureBridge.js	IPC 处理器注册	主进程
src/bridge/windowBridge.js	窗口通信	主进程
src/bridge/internalBridge.js	内部事件总线	主进程
src/ui/app/header.html	Header 窗口 HTML	渲染进程
src/ui/app/headerApp.tsx	React 入口	渲染进程
src/ui/app/HeaderControllerReact.tsx	状态控制器	渲染进程
src/ui/components/WelcomeHeader.tsx	欢迎界面	React 组件
src/ui/components/PermissionPanel.tsx	权限面板	React 组件
src/features/common/services/databaseInitializer.js	数据库初始化	服务
src/features/common/services/authService.js	认证服务	服务
src/features/common/services/modelStateService.js	模型状态管理	服务
src/features/common/services/ollamaService.js	Ollama 管理	服务
src/features/listen/listenService.js	实时转录服务	服务
src/features/ask/askService.js	AI 问答服务	服务
src/features/shortcuts/shortcutsService.js	快捷键管理	服务